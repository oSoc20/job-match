::style-typography [stylesheet]
p {
    font-size: var(--font-size-lg);
}

.story-container__text, .story-question {
    max-width: 70%;
    font-family: var(--font-type);

    font-weight: 600;
    font-size: 18px;
    line-height: 22px;
    text-align: center;
}

.story-question {
    margin: 30px 0 0 0;
}